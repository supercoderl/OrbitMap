export enum ReactionType {
    Like,
    Dislike,
    Love,
    Wow,
    Sad,
    Angry
}

export enum LevelType {
    Member,
    Silver,
    Gold,
    Diamond,
}

export enum PlaceType {
    Restaurant,
    Coffee,
    Hotel,
    HomeStay,
    Resort
}

export enum AnnotationType {
    Text,
    Time,
    Location,
    Weather
}